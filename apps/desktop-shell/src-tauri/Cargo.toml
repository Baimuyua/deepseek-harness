[package]
name = "dsh-desktop-shell"
version = "0.1.0"
description = "Tauri shell around the dsh web GUI (v1: sidecar + loopback URL)"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

[profile.dev]
opt-level = 1
