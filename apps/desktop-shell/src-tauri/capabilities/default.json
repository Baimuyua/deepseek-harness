{
  "$schema": "https://schema.tauri.app/acl/capability",
  "identifier": "default",
  "description": "The placeholder page only needs core defaults; the GUI itself loads from the loopback server without Tauri IPC.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
